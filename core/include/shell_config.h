/*
 * shell_config.h
 *
 * Compile-time configuration for the Generic Command Shell.
 */

#ifndef SHELL_CONFIG_H
#define SHELL_CONFIG_H

/** Maximum length of a command line (including the null terminator). */
#define MAX_COMMAND_LENGTH 50

/** Maximum number of arguments accepted by the tokenizer. */
#define MAX_ARGUMENTS 10

#endif /* SHELL_CONFIG_H */
